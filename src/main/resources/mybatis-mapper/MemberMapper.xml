<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.kream.member.dao.MemberDAO">
    <insert id="save" parameterType="com.flab.kream.member.dto.MemberDTO">
        INSERT INTO MEMBER
            (EMAIL,PASSWORD,NICKNAME,GENDER,BIRTH_DAY,IMAGE_URL,
             CREATED_DATE, CREATED_BY, UPDATED_DATE, UPDATED_BY)
        VALUES (#{email},#{password},#{nickName},#{gender},
                #{birthDate},#{imageUrl},now(),#{createBy},now(),#{updatedBy})
    </insert>

    <select id="findById" parameterType="Integer" resultType="com.flab.kream.member.dto.MemberDTO">
        SELECT MEMBER_ID
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
    </select>
</mapper>


